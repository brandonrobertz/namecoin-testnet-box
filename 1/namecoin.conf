# testnet-box functionality
regtest=1
dnsseed=0
upnp=0

# listen on different ports than default testnet
# and also different from bitcoin testnet box incase
# it's also running
port=18000
rpcport=18001

# always run a server, even with bitcoin-qt
server=1

# enable SSL for RPC server
#rpcssl=1

rpcallowip=0.0.0.0/0

rpcuser=admin1
rpcpassword=123
